version: "3.7"

####################### Services Definition #######################
services:
  client:
    container_name: client
    image: client:test
    build:
      context: .
      dockerfile: Dockerfile
      target: "test"
    ports:
      - 3000:3000
    environment:
      - PORT=3000
    networks:
      - default

####################### Networks Definition #######################
networks:
  default:
    driver: bridge
